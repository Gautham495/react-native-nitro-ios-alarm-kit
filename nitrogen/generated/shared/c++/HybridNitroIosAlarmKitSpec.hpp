///
/// HybridNitroIosAlarmKitSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2026 Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `CustomizableAlarmButton` to properly resolve imports.
namespace margelo::nitro::nitroiosalarmkit { struct CustomizableAlarmButton; }
// Forward declaration of `AlarmCountdown` to properly resolve imports.
namespace margelo::nitro::nitroiosalarmkit { struct AlarmCountdown; }
// Forward declaration of `AlarmWeekday` to properly resolve imports.
namespace margelo::nitro::nitroiosalarmkit { enum class AlarmWeekday; }

#include <NitroModules/Promise.hpp>
#include <string>
#include "CustomizableAlarmButton.hpp"
#include <optional>
#include "AlarmCountdown.hpp"
#include "AlarmWeekday.hpp"
#include <vector>

namespace margelo::nitro::nitroiosalarmkit {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `NitroIosAlarmKit`
   * Inherit this class to create instances of `HybridNitroIosAlarmKitSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridNitroIosAlarmKit: public HybridNitroIosAlarmKitSpec {
   * public:
   *   HybridNitroIosAlarmKit(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridNitroIosAlarmKitSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridNitroIosAlarmKitSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridNitroIosAlarmKitSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::shared_ptr<Promise<bool>> requestAlarmPermission() = 0;
      virtual std::shared_ptr<Promise<bool>> scheduleFixedAlarm(const std::string& title, const CustomizableAlarmButton& stopBtn, const std::string& tintColor, const std::optional<CustomizableAlarmButton>& secondaryBtn, std::optional<double> timestamp, const std::optional<AlarmCountdown>& countdown) = 0;
      virtual std::shared_ptr<Promise<bool>> scheduleRelativeAlarm(const std::string& title, const CustomizableAlarmButton& stopBtn, const std::string& tintColor, double hour, double minute, const std::vector<AlarmWeekday>& repeats, const std::optional<CustomizableAlarmButton>& secondaryBtn, const std::optional<AlarmCountdown>& countdown) = 0;
      virtual bool isAvailable() = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "NitroIosAlarmKit";
  };

} // namespace margelo::nitro::nitroiosalarmkit
